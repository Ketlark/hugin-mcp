services:
  searxng:
    image: searxng/searxng:latest
    container_name: hugin-mcp-searxng
    ports:
      - "${HUGIN_SEARXNG_PORT:-8888}:8080"
    volumes:
      - ./searxng-settings.yml:/etc/searxng/settings.yml:ro
    restart: unless-stopped
